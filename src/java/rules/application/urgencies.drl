import drools.createEmergency.Urgency

rule "new urgency register"

    dialect "java"
    when
        u1 : Urgency ()
    then 
        u1.____________;
        System.out.println("Rule new urgency register applied over " + u1);
end