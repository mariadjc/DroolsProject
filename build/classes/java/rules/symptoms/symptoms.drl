import droolsEvaluation.main

rule "heart_attack"

    dialect "java"
    when
        u1 : Urgency (examMark < 4)
    then 
        s1.setFinalMark(Student.Result.FAIL);
        System.out.println("Rule mark1 applied over " + s1);
end




